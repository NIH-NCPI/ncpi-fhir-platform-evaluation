import pytest
import pdb

from fhir_walk.model.patient import Patient

test_id = f"{'2.6.3':<10} - _query"

test_weight = 8

def test_query(host):
    assert 0 == 1, "TODO - Need a perform a _query against a custom operation"

